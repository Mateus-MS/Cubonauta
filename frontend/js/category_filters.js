var category = "F2L"
function setCategory(_category){
    /*TEMPORARIO*/
    var container = document.getElementById("container")
    category = _category
    container.scrollTo(0,0)
    formula_index = 0
}